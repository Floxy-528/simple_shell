#include "shell.h"
/**
 * main - this is the entry point of the simple shell program
 * Return: returns 0 on success
 */

int main(void)
{
    while (1)
    {
        readCommand();
    }

    return (0);
}
